import { GetWeekdayPipe } from './get-weekday.pipe';

describe('GetWeekdayPipe', () => {
  it('create an instance', () => {
    const pipe = new GetWeekdayPipe();
    expect(pipe).toBeTruthy();
  });
});
